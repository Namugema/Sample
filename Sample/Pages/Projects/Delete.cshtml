@page "{projectId}"
@model Sample.Pages.Projects.DeleteModel
@{
    //Layout = null;
}

        <h2>Delete!</h2>
        <div class="alert alert-danger">
            Are you sure you want to delete @Model.project.Name ?
        </div>
        <form method="post">
            <button type="submit" class="btn btn-danger">Yes!</button>
            <a asp-page="List" class="btn btn-secondary">Cancel</a>
        </form>
